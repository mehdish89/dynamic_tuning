class optimizer:

	def set_params(config):
		# update the values of parameters with the given configuration

		pass

	def set_params_config(config):
		# update the configuration of the parameters e.g. min, max, default values.

		pass

	def set_objective(func):
		# set the objective value that the optimizer uses for evaluation

		pass

	def __init__(self, config, func):
		# initializer
		# set the params configs and the objective function

		pass

